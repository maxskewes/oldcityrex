import React from 'react';
import { Box, Container, Text } from '@chakra-ui/react';
import PageHead from '../components/PageHead';

const Listen = () => {
  return (
    <Box>
      <PageHead pageHeading='Listen'></PageHead>
      <Container>
          <Text color='white'>Listen</Text>
      </Container>
    </Box>
  );
};

export default Listen;
